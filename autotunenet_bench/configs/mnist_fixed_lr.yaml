seed: 42

training:
  batch_size: 64
  epochs: 10
  initial_lr: 0.01

controller:
  type: fixed_lr

  lr: 0.01

  parameter_space:
    lr: [0.01, 0.01]

  metrics:
    smoothing_window: 3

non_stationarity:
  type: label_noise
  start_epoch: 5
  noise_ratio: 0.3
